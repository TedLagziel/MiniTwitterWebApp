@page
@model MiniTwitterWebApp.Areas.Identity.Pages.Tweet.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tweet[0].Profile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tweet[0].Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tweet[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Tweet)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Profile.DisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>

                <td>
                    @*<a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>
