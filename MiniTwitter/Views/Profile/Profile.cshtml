@page
@model MiniTwitter.Views.Profile.ProfileModel
@*@{
    ViewData["TwitterUser.DisplayName"] = "Index";
}*@

<h1>Index</h1>

<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tweets[0].TwitterUser.DisplayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tweets[0].Content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tweets[0].Date)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Tweets) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TwitterUser.DisplayName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date.ToLongTimeString())
            </td>
            @*<td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>*@
        </tr>
    }
    </tbody>
</table>